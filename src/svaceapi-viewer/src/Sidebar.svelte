<!-- <script>
    import { currentSelected } from "./stores.ts";

    let currentContext = "";

    function addContext(ctxt) {
        currentContext = ctxt;
    }
</script>

<div class="sidebar">
    {#if $currentSelected}
        <h1>Hooray</h1>
        <p>$ {$currentSelected.layerX}</p>
    {:else}
        <p>No edge selected</p>
    {/if}
</div> -->
